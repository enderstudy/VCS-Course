/**
* This function will run when the page loads, once the browser
* has detected we're ready to go it'll log "hello world to the console"
*/
(function() {
    const date = new Date();
    const name = "Mousey";
    console.log('hello ' + name.toUpperCase());
});